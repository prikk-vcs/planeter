#![forbid(unsafe_code)]
//! `planeter-runner` — the planeter CI runner agent, run on separate hosts. Lands at M4 (RFC 006);
//! until then this binary is a placeholder and is not published.

fn main() {
    // Not yet implemented: see rfcs/accepted/006-ci-runners.md.
}
