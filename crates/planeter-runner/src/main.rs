#![forbid(unsafe_code)]
//! `planeter-runner` — the planeter CI runner agent, run on separate hosts (design phase).

fn main() {
    // Design phase: not yet implemented. See docs/src/ (design set) and rfcs/ (RFC 001-009).
}
