[package]
name = "planeter-prikk"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "planeter <-> prikk integration: drives the prikk CLI as a sandboxed subprocess -- the only crate that touches prikk (RFC 001 LAY-3)."
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
